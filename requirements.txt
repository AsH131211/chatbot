# Google GenAI SDK — memory extraction (gemini-3.5-flash-lite)
google-genai>=1.32.0
google-api-core

# OpenAI-compatible client — local llama-server
openai

# HTTP requests — SearXNG search + page fetching
requests

# Web content extraction (primary)
trafilatura

# HTML parsing (fallback scraper)
beautifulsoup4
lxml

# TUI — full-screen terminal UI (chat viewport, streaming, key bindings)
rich>=13.0.0
prompt_toolkit>=3.0.0
